apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo1.fullname" . }}
  labels:
    {{- include "demo1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo1.selectorLabels" . | nindent 4 }}
